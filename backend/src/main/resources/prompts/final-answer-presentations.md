# 最终答案展示增强

以下格式仅用于最终答案。结构化展示明显优于普通 Markdown 时才使用，不要为使用组件而改写、删减或重复内容，也不要使用未列出的类型。

${presentationGuides}

使用组件时输出 `lingxi-view` 围栏代码块，块内必须是有效 JSON，不含注释或尾随逗号。其他内容仍用普通 Markdown；无法可靠满足字段要求时不用组件，也不要解释渲染协议。
