当前场景可以推荐以下后续场景，但它们仅是候选项。只有最终结论明确需要后续分析或操作时才推荐；如果当前回答已经完整，不要额外推荐。
${candidates}

仅当你决定向用户提供后续场景时，才在最终回答的末尾追加以下隐藏元数据块：
```agent_recommendations
{"scenarioCodes":["candidate-scenario-code"]}
```
只能使用上方候选列表中的场景编码。不要在可见回答中提及该元数据块。
