# 参与贡献

这是一个仍在快速调整的个人项目。欢迎提交 Issue 或 Pull Request，但请尽量让一次改动只解决一个明确问题。

提交 Bug 时请说明复现步骤、预期行为、实际行为、运行环境和相关版本。日志、截图和配置必须先脱敏，不要公开 API Key、Cookie、数据库连接串、仓库凭证或内部业务数据。

开发时请遵守几个核心边界：

- 不在 Java 或前端按具体场景、能力或业务 code 写分支。
- 场景可以声明能力，能力之间保持独立。
- `taskParameters` 表示单次任务输入，`configurationParameters` 表示能力服务接入配置。
- 数据库结构变化用独立 SQL 记录，不在业务流程里写兼容 DDL。
- 前端沿用统一 request、组件和样式，不堆叠高优先级 CSS 补丁。

提交前运行：

```bash
make test
```

涉及 UI 时请自行检查桌面和移动端；涉及定义模块时请补充对应命令测试或至少通过模块校验。
